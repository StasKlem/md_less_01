# LLM Chat Client

Консольное TUI-приложение на Go для общения с Large Language Models через API с поддержкой потокового вывода токенов.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Go](https://img.shields.io/badge/go-1.21+-00ADD8?logo=go)

## Возможности

- 🖥️ **Интерактивный TUI интерфейс** на базе Bubble Tea
- ⚡ **Потоковый вывод** ответов (токены отображаются по мере поступления)
- 💬 **История диалога** с поддержкой контекста
- ⚙️ **Гибкая конфигурация** через JSON файл, CLI флаги и переменные окружения
- 🎛️ **Команды в чате** для изменения параметров на лету
- 🔐 **Безопасность** — API ключ через переменную окружения `ROUTERAI_API_KEY`
- 📝 **Структурированное логирование** с уровнями (debug, info, warn, error)
- 🔄 **Поддержка OpenAI-compatible API** — Ollama, vLLM, OpenAI и др.
- ✅ **Покрытие тестами** >70%
- 🏗️ **Чистая архитектура** с разделением на пакеты

## Быстрый старт

### 1. Установка зависимостей

```bash
go mod tidy
```

### 2. Настройка API ключа

```bash
export ROUTERAI_API_KEY="your-api-key-here"
```

### 3. Запуск

```bash
# Сборка
go build -o llm-client .

# Запуск с конфигом по умолчанию
./llm-client

# С указанием адреса сервера
./llm-client -address http://localhost:11434 -model llama3
```

## Требования

- Go 1.21 или новее
- Терминал с поддержкой Unicode
- Доступ к LLM серверу (Ollama, vLLM, OpenAI API и др.)

## Структура проекта

```
.
├── cmd/
│   └── llm-client/       # Бинарное приложение (старый main.go)
├── internal/
│   ├── chat/             # Модели данных и история диалога
│   │   ├── chat.go       # ChatHistory, Message, Role
│   │   └── chat_test.go
│   ├── client/           # HTTP клиент для LLM API
│   │   ├── client.go     # Client, ChatRequest, ChatStream
│   │   └── client_test.go
│   ├── config/           # Конфигурация приложения
│   │   ├── config.go     # Config, ServerConfig, ModelConfig
│   │   └── config_test.go
│   ├── errors/           # Типизированные ошибки
│   │   ├── errors.go     # AppError, ErrorKind
│   │   └── errors_test.go
│   ├── logger/           # Структурированное логирование
│   │   ├── logger.go     # Logger, Config
│   │   └── logger_test.go
│   └── ui/               # TUI компоненты
│       ├── ui.go         # Model, View, Update
│       └── ui_test.go
├── main.go               # Точка входа, dependency injection
├── config.json           # Файл конфигурации
├── go.mod                # Зависимости Go модуля
├── go.sum                # Хеш-суммы зависимостей
├── .golangci.yml         # Конфигурация линтера
├── README.md             # Эта документация
└── CONFIG.md             # Подробная документация конфигурации
```

## Архитектура

Приложение использует чистую архитектуру с разделением ответственности:

1. **UI Layer** (`internal/ui`) — Bubble Tea модель, обработка ввода, рендеринг
2. **Client Layer** (`internal/client`) — HTTP-запросы, парсинг SSE-стрима, контексты
3. **Domain Layer** (`internal/chat`) — модели данных, история диалога
4. **Config Layer** (`internal/config`) — конфигурация, валидация, viper
5. **Logger Layer** (`internal/logger`) — структурированное логирование (slog)
6. **Errors Layer** (`internal/errors`) — типизированные ошибки

### Dependency Injection

Все зависимости внедряются через конструкторы:

```go
// Создание логгера
log := logger.NewLogger(logger.Config{
    Enabled:  true,
    FilePath: "/tmp/llm.log",
    Level:    logger.LevelDebug,
})

// Создание клиента с зависимостями
client := client.NewClient(
    cfg.Server.Address,
    cfg.Server.APIEndpoint,
    client.WithLogger(log),
    client.WithTimeout(60*time.Second),
)

// Создание UI модели
model := ui.NewModel(cfg, ui.WithLogger(log))
```

## Конфигурация

### Файл конфигурации

Приложение поддерживает загрузку конфигурации из JSON файла.

```bash
# Создать файл конфигурации по умолчанию
./llm-client -init-config

# Запустить с указанием файла конфигурации
./llm-client -config config.json
```

### Пример config.json

```json
{
  "server": {
    "address": "http://localhost:11434",
    "api_endpoint": "/v1/chat/completions"
  },
  "model": {
    "name": "llama3",
    "system_prompt": "You are a helpful assistant.",
    "temperature": 0.7,
    "top_p": 0.9,
    "max_tokens": 0,
    "stream": true
  },
  "ui": {
    "show_timestamps": false,
    "theme": "dark",
    "scroll_speed": 10
  },
  "log": {
    "enabled": true,
    "file_path": "/tmp/llm-client.log",
    "level": "info",
    "log_requests": true,
    "log_responses": true
  }
}
```

### Переменные окружения

| Переменная | Описание |
|------------|----------|
| `ROUTERAI_API_KEY` | API ключ для аутентификации |
| `LLM_CLIENT_CONFIG` | Путь к файлу конфигурации |
| `LLM_CLIENT_LOG` | Путь к файлу логов |
| `LLM_CLIENT_ADDRESS` | Адрес сервера |
| `LLM_CLIENT_MODEL` | Имя модели |
| `LLM_CLIENT_TEMPERATURE` | Температура (0.0-2.0) |
| `LLM_CLIENT_TOP_P` | Top P параметр (0.0-1.0) |

### Флаги командной строки

| Флаг | Описание |
|------|----------|
| `-config <path>` | Путь к файлу конфигурации |
| `-address <url>` | Адрес LLM сервера |
| `-model <name>` | Имя модели |
| `-system <text>` | Системный промпт |
| `-temperature <float>` | Температура (0.0-2.0) |
| `-top-p <float>` | Top P параметр (0.0-1.0) |
| `-show-config` | Показать конфигурацию по умолчанию |
| `-init-config` | Создать файл конфигурации |
| `-version, -v` | Показать версию |

## Использование

### Базовый запуск

```bash
./llm-client
```

### С флагами

```bash
./llm-client \
  -address http://localhost:11434 \
  -model mistral \
  -temperature 0.8 \
  -top-p 0.95
```

### С логированием

```bash
export LLM_CLIENT_LOG=/tmp/llm-client.log
./llm-client
```

## Команды в чате

| Команда | Описание | Пример |
|---------|----------|--------|
| `/set <param> <value>` | Изменить параметр | `/set temperature 0.9` |
| `/clear` | Очистить историю | `/clear` |
| `/config` | Показать настройки | `/config` |
| `/save` | Сохранить настройки | `/save` |
| `/help` | Показать справку | `/help` |
| `/stream` | Переключить режим стрима | `/stream` |
| `/exit` | Выйти | `/exit` |

### Параметры для `/set`

- `temperature` или `temp` — температура (0.0-2.0)
- `top_p` или `top-p` — параметр top_p (0.0-1.0)
- `model` — имя модели
- `system` или `system_prompt` — системный промпт
- `stream` — режим стриминга (true/false)

## Управление в интерфейсе

| Клавиша | Действие |
|---------|----------|
| `Enter` | Отправить сообщение |
| `↑` / `↓` | Скролл истории |
| `PgUp` / `PgDn` | Быстрый скролл |
| `Ctrl+C` | Прервать генерацию / Выход |

## Примеры

### Подключение к Ollama

```bash
# Запустите Ollama
ollama serve
ollama pull llama3

# Запустите клиент
./llm-client -address http://localhost:11434 -model llama3
```

### Подключение к OpenAI

```bash
export ROUTERAI_API_KEY="$OPENAI_API_KEY"

./llm-client \
  -address https://api.openai.com \
  -model gpt-3.5-turbo
```

### Логирование запросов/ответов

```bash
export LLM_CLIENT_LOG=/tmp/llm-debug.log
./llm-client

# Просмотр лога (JSON формат)
cat /tmp/llm-debug.log | jq
```

## Тестирование

### Запуск всех тестов

```bash
go test ./...
```

### Запуск тестов с покрытием

```bash
go test -cover ./...
```

### Запуск тестов с подробным выводом

```bash
go test -v ./...
```

### Генерация отчёта о покрытии

```bash
go test -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html
```

## Сборка

### Debug

```bash
go build -o llm-client .
```

### Release

```bash
CGO_ENABLED=0 go build -ldflags="-s -w" -o llm-client .
```

### Кросс-компиляция

```bash
GOOS=linux GOARCH=amd64 go build -o llm-client-linux .
GOOS=darwin GOARCH=amd64 go build -o llm-client-macos .
GOOS=windows GOARCH=amd64 go build -o llm-client.exe .
```

## Линтинг

### Установка golangci-lint

```bash
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
```

### Запуск линтера

```bash
golangci-lint run
```

### Запуск с автоисправлением

```bash
golangci-lint run --fix
```

## Форматирование

```bash
# Форматировать все файлы
gofmt -w .

# Оптимизировать импорты
goimports -w .
```

## Зависимости

- [bubbletea](https://github.com/charmbracelet/bubbletea) — TUI фреймворк
- [lipgloss](https://github.com/charmbracelet/lipgloss) — стилизация терминала
- [viper](https://github.com/spf13/viper) — конфигурация
- [slog](https://pkg.go.dev/log/slog) — структурированное логирование (стандартная библиотека)

## Вклад в проект

1. Fork репозитория
2. Создайте ветку (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Лицензия

MIT License
